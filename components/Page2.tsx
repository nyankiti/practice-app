import React from "react";
import { StyleSheet, View } from "react-native";

interface PageProps {
  title: string;
  index: number;
}

const Page2: React.FC<PageProps> = ({ title, index }) => {
  return <View></View>;
};

export default Page2;

const styles = StyleSheet.create({});
